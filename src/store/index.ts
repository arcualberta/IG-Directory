import { createStore } from 'vuex'

import { search } from '@arcualberta/catfish-ui';

export default createStore({
    modules: {
        search
    },
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
})

